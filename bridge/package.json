{
  "name": "smart-box-bridge",
  "private": true,
  "description": "Serves the web UI and forwards WebSocket <-> Arduino serial (fixed USB path).",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "serialport": "^12.0.0",
    "ws": "^8.18.0"
  }
}
